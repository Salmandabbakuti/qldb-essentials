export declare class SharedSymbolTable {
    private readonly _name;
    private readonly _version;
    private readonly _symbols;
    protected readonly _idsByText: Map<string, number>;
    constructor(_name: string, _version: number, _symbols: string[]);
    protected _numberOfSymbols: number;
    readonly numberOfSymbols: number;
    readonly name: string;
    readonly version: number;
    getSymbolText(symbolId: number): string;
    getSymbolId(text: string): number;
}
