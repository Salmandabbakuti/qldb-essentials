import JSBI from "jsbi";
export declare class JsbiSerde {
    private static readonly SERIALIZED_JSBI_SIZES_TO_PRECOMPUTE;
    private static readonly BITS_PER_BYTE;
    private static readonly BYTE_MAX_VALUE;
    private static readonly SIZE_THRESHOLDS;
    static toSignedIntBytes(value: JSBI, isNegative: boolean): Uint8Array;
    static fromUnsignedBytes(bytes: Uint8Array): JSBI;
    static toUnsignedIntBytes(value: JSBI): Uint8Array;
    static getUnsignedIntSizeInBytes(value: JSBI): number;
    private static calculateSizeThresholds;
    private static calculateSizeThreshold;
}
