export declare class Writeable {
    private bufferSize;
    private buffers;
    private index;
    private clean;
    constructor(bufferSize?: number);
    readonly currentBuffer: Uint8Array;
    readonly totalSize: number;
    writeByte(byte: number): void;
    writeBytes(buf: Uint8Array, offset?: number, length?: number): void;
    getBytes(): Uint8Array;
}
